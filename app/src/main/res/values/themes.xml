<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      windowBackground auf den Grundton der Palette: Ohne das blitzt beim Start das weiße
      Standardfenster auf, bevor Compose das erste Bild zeichnet.
    -->
    <style name="Theme.ShortBlock" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
